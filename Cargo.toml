[package]
name = "tangram-protos"
version = "0.0.1"
edition = "2021"
description = "A crate for interacting with the tangram protos."

[dependencies]
protobuf = "3.4.0"

[build-dependencies]
glob = "0.3.1"
protobuf-codegen = "3.2.0"
