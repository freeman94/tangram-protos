[package]
name = "tangram-protos"
version = "0.2.0"
edition = "2021"
description = "A crate for interacting with the tangram protos."

[features]
ros = []

[dependencies]
protobuf = "3.4.0"

[build-dependencies]
glob = "0.3.1"
protobuf-codegen = "3.2.0"
